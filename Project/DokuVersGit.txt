================================================================================
DokuVersGit.txt - Dokumentation, Versionierung und Git
================================================================================
Stand: 4.1.6

Dieses Dokument ist die einzige aktive Regelquelle fuer Dokumentation,
Versionierung und Git im ePiber-Projekt. Alle Pfade sind relativ zum
Repository-Root, sofern sie nicht mit / beginnen.

Die Regeln gelten ab der ersten vollstaendig angeglichenen Version 3.1.11. Aeltere Commits
koennen von diesen Regeln abweichen und werden nicht nachtraeglich umgeschrieben.


================================================================================
1. GRUNDREGELN UND FREIGABEN
================================================================================

1. Dokumentation, Versionierung sowie Git-Aktionen werden nur auf ausdruecklichen
   Auftrag des Users ausgefuehrt.
2. Ein Implementierungsauftrag allein autorisiert keine Dokumentation, keinen
   Commit, keinen Push und keinen Merge.
3. Ein eindeutiger Auftrag gilt fuer genau die genannten Schritte. Bei unklarem
   oder erweitertem Umfang ist vor der Ausfuehrung nachzufragen.
4. Wenn der User "dokumentieren" anfordert, wird zuerst nur das temporaere
   Changelog des aktuellen Arbeitszweigs aktualisiert. Nach einem gegebenenfalls
   noch notwendigen Branchabschluss muss immer gefragt werden, ob eine neue
   Main-Version erstellt werden soll.
5. Vor einer Versionierung werden aktuelle Main-Version, erkannte SemVer-Stufe,
   vorgeschlagene Zielversion und Begruendung genannt. Ohne Bestaetigung der
   konkreten Zielversion wird keine Version erstellt.
6. Die Bestaetigung einer konkreten Version autorisiert:
   - Uebernahme der temporaeren Changelog-Eintraege,
   - Aktualisierung der betroffenen Fachdokumentation,
   - Aktualisierung von Backend/package.json und Backend/package-lock.json.
   Ein Commit wird dadurch nicht autorisiert und benoetigt immer eine eigene
   ausdrueckliche Aufforderung.
7. Push benoetigt auch nach einer Versionsbestaetigung eine eigene ausdrueckliche
   Freigabe. Seitenbranches werden weder lokal noch remote geloescht.
8. Aendert sich nach der Bestaetigung der Main-Stand, der Aenderungsumfang oder
   eine konfliktbedingte Loesung, verfaellt die Bestaetigung. Version und Umfang
   muessen erneut vorgeschlagen und bestaetigt werden.
9. Keine destruktiven Git-Aktionen, kein Force-Push, kein Rebase eines
   veroeffentlichten Branches, kein Amend und kein Ueberspringen von Pruefungen
   ohne gesonderten ausdruecklichen Auftrag.
10. Wird bei einer Analyse oder Umsetzung eine dauerhaft relevante fachliche
     Information erkannt, die in der kanonischen Dokumentation fehlt, wird dem
     User vor ihrer Aufnahme ein konkreter Dokumentationsvorschlag genannt. Der
     Vorschlag enthaelt mindestens die vorgesehene Zieldatei und die einzutragende
     Aussage. Erst nach Ruecksprache wird die permanente Dokumentation angepasst.
     Verpflichtende Eintraege im temporaeren Branch-Changelog werden weiterhin
     ohne gesonderte Einzelrueckfrage im freigegebenen Auftragsumfang gepflegt.
11. Bei wiederkehrenden OpenCode-Arbeitsablaeufen mit klar getrennten Phasen aus
    Vorbereitung, Umsetzung und Abschluss soll vor Beginn einer neuen Phase knapp
    geprueft werden, ob ein Modell- oder Reasoning-Wechsel sinnvoll ist. Ziel ist
    ein geringerer Kontext- und Kontingentverbrauch, ohne das fuer die Phase
    benoetigte Kontextfenster zu unterschreiten. Leichtgewichtigere Modelle sind
    fuer regelgebundene Vorbereitungs- und Abschlussroutinen bevorzugt; fuer
    Analyse, Umsetzung und schwierige Pruefungen bleiben staerkere Modelle
    vorgesehen.

Verpflichtender Beginn jeder neuen Arbeit:

1. Jede neue Aufgabe oder Aenderung beginnt bei einem vollstaendig committeden
   Arbeitsbaum zwingend mit einem neuen `-x`-Entwicklungsstand. Fachliche,
   organisatorische oder technische Dateien duerfen vorher nicht geaendert werden.
2. Zuerst werden Branch, Status und letzter Commit bestimmt.
3. In einem Seitenbranch wird unabhaengig vom noch unbekannten Aenderungsumfang
   nur die laufende Branch-Commitnummer erhoeht. Backend/package.json, das
   Root-Paket in Backend/package-lock.json und der offene Abschnitt des
   Branch-Changelogs werden gemeinsam auf
   `<naechste-Branch-Commit-ID>-x` gesetzt.
4. Auf main muss vor jedem `-x`-Stand und vor jeder sonstigen Aenderung gefragt
   werden, ob ein Seitenbranch angelegt werden soll.
5. Bestaetigt der User den Seitenbranch, wird zuerst der freie Branchname nach
   Abschnitt 4 bestimmt und der Branch angelegt. Danach wird dessen erste
   Branch-Commit-ID plus `-x` nach Punkt 3 vorbereitet.
6. Der erste Commit auf einem neuen Seitenbranch ist die vorbereitende Versions-
    Reserve. Er setzt die Paket- und Changelog-Version auf
    `<naechste-Branch-Commitnummer>-x` und hat z.B. die Form
   `<branchname>-1 | Branch-Stand auf -x initialisiert`.
7. Der fachliche erste Commit darf erst nach dieser Vorbereitungs-Commit erfolgen
   und entfernt danach das `-x` in der finalen Commit-Version.
8. Lehnt der User den Seitenbranch ab, darf nur nach ausdruecklicher Freigabe der
   direkten Main-Ausnahme weitergearbeitet werden. Dann wird die aktuelle
   Main-Patchversion um eins erhoeht und mit `-x` als vorlaeufiger Arbeitsstand
   in package.json, package-lock.json und ChangeLog-main.tmp gesetzt.
9. Enthaelt der Auftrag die jeweils erforderliche Versionsvorbereitung nicht
   ausdruecklich, muss vor jeder Aenderung gefragt werden, ob der verpflichtende
   `-x`-Entwicklungsstand angelegt werden soll. Ohne Bestaetigung wird die Aufgabe
   nicht umgesetzt.
10. Ein Verzicht auf den `-x`-Stand ist nicht zulaessig. Die Rueckfrage dient der
   notwendigen Freigabe fuer Versionierungs- und Dokumentationsdateien, nicht der
   Wahl zwischen Arbeit mit oder ohne `-x`.
11. Erst nach dem vollstaendig angelegten und geprueften `-x`-Stand beginnen die
   eigentlichen Aenderungen.


================================================================================
2. VERSION UND MIGRATIONSBASIS
================================================================================

Versionsformat:

  MAJOR.MINOR.PATCH

Die einzige kanonische Quelle ist:

  Backend/package.json -> Feld "version"

Die laufende Version wird je System ueber GET /version ausgegeben. Auf main
muessen Paketversion und Versionsnummer im letzten Versionscommit uebereinstimmen.
In einem sauberen, committed Seitenbranch muessen Paketversion und vollstaendige
Branch-Commit-ID uebereinstimmen. Waehrend beabsichtigte, noch nicht committe
Branch-Arbeit existiert, muss die Paketversion stattdessen die naechste
Branch-Commit-ID mit dem Zusatz `-x` ausweisen. Andere Abweichungen blockieren
weitere Commits oder Versionierungen, bis der User die Korrektur bestaetigt hat.

Abgeschlossene Migration mit Version 3.1.11:

  - Vor der Migration war 3.1.10 als logische Main-Version bestaetigt, waehrend
    Backend/package.json historisch noch 3.0.2 enthielt.
  - Der Bootstrap-Branch 3.1.10-paj-1 uebernahm die bereits begonnenen
    Dokumentationsaenderungen und wurde mit dem bestaetigten Merge 3.1.11
    integriert.
  - Seit 3.1.11 stimmen Paket-, Commit- und Changelog-Version wieder ueberein.
  - Die Bootstrap-Ausnahme ist abgeschlossen und darf nicht wiederholt werden.

In Seitenbranches wird keine neue Main-SemVer-Version vergeben. Stattdessen wird
vor jedem Branch-Commit die Paketversion auf die vollstaendige Commit-ID gesetzt:

  <Branchname>-<laufendeNummer>

Beispiel:

  Branch:        3.1.12-paj-1
  zweiter Commit 3.1.12-paj-1-2 | Kommunikationsarchitektur abgesichert
  Paketversion:  3.1.12-paj-1-2

Backend/package.json und das Root-Paket in Backend/package-lock.json muessen vor
dem Commit denselben Wert erhalten. GET /version weist dadurch auf Testsystemen
exakt den installierten Branch-Commit aus. Beim naechsten Branch-Commit wird die
laufende Nummer in Commit-Nachricht und Paketdateien gemeinsam erhoeht.

Entwicklungsstand vor dem Commit:

  <Branchname>-<naechsteLaufendeNummer>-x

Beispiel nach dem abgeschlossenen Commit `3.1.12-paj-1-2`:

  beabsichtigte uncommittete Arbeit  3.1.12-paj-1-3-x
  geplanter finaler Commit          3.1.12-paj-1-3

Der literale Zusatz `-x` signalisiert einen veraenderten, testbaren, aber noch
nicht commitfaehigen Arbeitsstand. Er wird gleichzeitig in package.json, im
Root-Paket des package-lock.json und im offenen Abschnitt des temporaeren
Branch-Changelogs gesetzt. Nach Deployment beziehungsweise Prozessneustart muss
GET /version diesen `-x`-Stand auf dem Testsystem anzeigen.

In einem Seitenbranch reserviert `-x` ausschliesslich die naechste laufende
Branch-Commitnummer. Der fachliche Umfang der bevorstehenden Aenderung und eine
spaetere Main-SemVer-Stufe spielen dafuer keine Rolle. Der erste Commit des
Branches ist regelmaessig die vorbereitende `-x`-Version auf `-1`.

Bei einer ausdruecklich freigegebenen direkten Main-Ausnahme wird als
vorlaeufiger Arbeitsstand immer die naechste Patchversion verwendet:

  aktuelle Main-Version  4.1.1
  vorlaeufige Arbeit      4.1.2-x

Dieser Wert nimmt die spaetere SemVer-Bewertung nicht vorweg. Nach bekanntem
Aenderungsumfang kann er bei der Finalisierung beispielsweise zu 4.1.2, 4.2.0
oder 5.0.0 werden.

In einem Seitenbranch wird die naechste laufende Nummer durch `-x` nur reserviert
und erst durch den finalen Commit verbraucht. Werden die Aenderungen vollstaendig
verworfen, werden Paket- und Lockfile-Version auf die aktuelle HEAD-Commit-ID
zurueckgestellt und der offene Changelogabschnitt entfernt. Bei einer direkten
Main-Ausnahme werden stattdessen die Paketdateien auf die aktuelle Main-Version
zurueckgestellt und ChangeLog-main.tmp entfernt.

Im Seitenbranch wird `-x` erst auf die ausdrueckliche Aufforderung
`dokumentieren` in package.json, package-lock.json und Changelog entfernt. Bei
einer direkten Main-Ausnahme geschieht dies nach `dokumentieren` erst mit der
Bestaetigung der konkreten SemVer-Zielversion. Danach duerfen nur noch
Abschlusspruefung, Vollstaendigkeitskontrolle, Staging und der freigegebene Commit
folgen. Scheitert der Commit oder sind davor weitere inhaltliche Aenderungen
erforderlich, muss vor deren Beginn wieder auf den passenden `-x`-Stand
zurueckgestellt werden. Kein Commit und keine Main-Version darf `-x` enthalten.

Uebergangsausnahme fuer den bei Einfuehrung dieser Regel bereits vorhandenen
Commit `3.1.12-paj-1-1`: Er erbte noch die Main-Paketversion 3.1.12. Die neue
Branch-Paketversion gilt verbindlich ab `3.1.12-paj-1-2`; der historische Commit
wird weder umgeschrieben noch amended.


================================================================================
3. SEMANTIC VERSIONING
================================================================================

  Stufe  | Ausloeser
  -------|--------------------------------------------------------------------
  MAJOR  | Inkompatible DB-/API-Aenderung oder inkompatibler Architekturumbau
  MINOR  | Neues Feature, neue Seite oder neuer Endpoint
  PATCH  | Bugfix, Refactoring, Performance, Dokumentation, Style oder Tests

Bei mehreren Aenderungsarten gilt die hoechste erforderliche Stufe.

Beispiele ab 3.1.11:

  PATCH -> 3.1.12
  MINOR -> 3.2.0
  MAJOR -> 4.0.0

Der Vorschlag ist eine fachliche Empfehlung. Der User kann eine andere gueltige
SemVer-Version vorgeben; diese konkrete Nummer gilt mit seiner Antwort als
Bestaetigung.


================================================================================
4. BRANCH-MODELL
================================================================================

Der Normalfall ist die Arbeit in einem Seitenbranch. Direkte Aenderungen auf
main sind nur als ausdruecklich freigegebene Ausnahme erlaubt.

Aktuelle Entwickler-/Systemkuerzel:

  paj   Entwicklungs-WebRoot /srv/http/ePiber/paj
  pk    Entwicklungs-WebRoot /srv/http/ePiber/pk

Branchname:

  <Basisversion>-<Kuerzel>-<BranchId>

Beispiel:

  3.1.10-paj-1

Regeln:

1. Basisversion ist die aktuelle Main-Version beim Anlegen des Branches.
2. BranchId ist eine positive Ganzzahl. Fuer dieselbe Kombination aus
   Basisversion und Kuerzel wird die naechste historisch noch nie verwendete ID
   eingesetzt. Jede einmal verwendete ID bleibt dauerhaft vergeben.
3. Vor dem Anlegen werden lokale und Remote-Branches, die Git-Historie und
   ChangeLog-main.txt auf bereits verwendete Namen geprueft.
4. Der Branchname bleibt unveraendert, wenn main waehrend der Arbeit fortschreitet.
5. Mehrere Seitenbranches derselben Basisversion sind erlaubt. Sie werden
   nacheinander integriert; vor jedem Merge wird die Version neu berechnet.
6. Ein integrierter Branch bleibt lokal und remote dauerhaft als historischer
   Stand erhalten. Er wird weder geloescht noch fuer neue Arbeiten wiederverwendet.
7. Liegen noch uncommittete Aenderungen auf main, werden sie vor dem ersten
   Commit auf einen freigegebenen Seitenbranch uebernommen.


================================================================================
5. COMMIT-NAMEN
================================================================================

Commit im Seitenbranch:

  <Branchname>-<laufendeNummer> | <Kurzkommentar>

Beispiele:

  3.1.10-paj-1-1 | Dokumentationsworkflow vereinheitlicht
  3.1.10-paj-1-2 | Changelog-Historie nachgetragen

Die laufende Nummer beginnt je Branch bei 1 und wird lueckenlos erhoeht. Auch
ein erforderlicher Merge von main in den Seitenbranch erhaelt die naechste
Branch-Commitnummer. Branch-Commits benoetigen einen ausdruecklichen Auftrag.
Der erste Commit ist im Normalfall die vorbereitende `-x`-Version mit der
Commit-ID `...-1-x`.

Main- oder Merge-Commit:

  <NeueVersion> | <Kurzkommentar>

Beispiel:

  3.1.11 | Dokumentations- und Versionsworkflow vereinheitlicht

Mit "Commit-ID" ist in diesem Workflow die Commit-Nachricht gemeint, nicht der
von Git erzeugte SHA-Hash. Der Kurzkommentar beschreibt den gesamten Umfang in
einem knappen Satz.

Es werden keine Versions-Tags erstellt, solange der User dies nicht gesondert
beauftragt.


================================================================================
6. CHANGELOG-DATEIEN
================================================================================

Alle aktiven Changelog-Dateien liegen exakt unter Project/ChangeLogs/.
Gross-/Kleinschreibung ist unter Linux verbindlich.

Zentrales, versioniertes Changelog:

  Project/ChangeLogs/ChangeLog-main.txt

Temporaeres Log einer ausdruecklich erlaubten direkten Main-Aenderung:

  Project/ChangeLogs/ChangeLog-main.tmp

Temporaeres, versioniertes Seitenbranch-Log:

  Project/ChangeLogs/ChangeLog-<voller-Branchname>.txt

Beispiel:

  Project/ChangeLogs/ChangeLog-3.1.10-paj-1.txt

Regeln:

1. Jeder Seitenbranch besitzt spaetestens im ersten Commit ein eigenes Branch-Log.
   Es ist obligatorisches Commit-Bookkeeping, wird vor jedem Branch-Commit
   aktualisiert und ist durch die Freigabe dieses Commits mitautorisiert.
2. ChangeLog-main.tmp wird nur bei einer explizit erlaubten direkten Main-Arbeit
   verwendet, bleibt lokal und wird ueber .gitignore ausgeschlossen.
3. ChangeLog-main.txt enthaelt nur bestaetigte Main-Versionen. Unversionierte
   Aenderungen stehen dort nicht.
4. Beim Versionssprung werden alle zutreffenden Eintraege aus dem temporaeren Log
   uebernommen, geprueft und zusammengefasst. Danach wird das temporaere Log
   entfernt.
5. Bei einem Merge wird die Quelle als voller Seitenbranchname dokumentiert.
6. Software, Infrastruktur, Dokumentation und Repository-Aenderungen stehen im
   selben zentralen Changelog. Es gibt keine weiteren aktiven Changelogs.
7. Leere Kategorien werden weggelassen. Doppelte Eintraege werden beim Uebertrag
   zusammengefuehrt, ohne Informationen zu verlieren.
8. Das Branch-Log wird commitweise gefuehrt. Jeder Abschnitt nennt mindestens
   Branch-Commit-ID, Datum und die tatsaechlichen Added-, Changed-, Fixed- und
   Removed-Punkte dieses Commits. Spaetere Korrekturen ersetzen die Historie eines
   frueheren Branch-Commits nicht, sondern werden im neuen Abschnitt ergaenzt.
9. Solange der Branch nicht als Main-Version integriert ist, ist das Branch-Log
   die einzige fachliche Dokumentation des neuen Anwendungsstands. Es muss deshalb
   auch Migrationen, Sicherheitsfolgen, Betriebsanforderungen, manuelle Restarbeit
   und bekannte Einschraenkungen nachvollziehbar enthalten.
10. Jeder Branch mit Auswirkungen auf permanente Dokumentation fuehrt am Ende des
    Logs einen Abschnitt "Dokumentationsuebernahme beim Main-Versionssprung". Dort
    werden fuer jede Zieldatei der exakte Pfad und die zu uebernehmenden Fakten,
    Ersetzungen, Entfernungen und neuen Abschnitte festgehalten. Sammelangaben wie
    "Doku aktualisieren" sind unzulaessig.
11. Die Uebernahmeliste ist vor jedem Branch-Commit gegen den aktuellen Gesamtdiff
    zu pruefen und fortzuschreiben. Dadurch darf zwischen Branch-Implementierung
    und spaeterer Main-Dokumentation keine Information verloren gehen.
12. Mit der ersten beabsichtigten Aenderung nach einem Branch-Commit wird ein
    offener Abschnitt fuer `<naechste-Commit-ID>-x` angelegt und waehrend der
    Entwicklung laufend fortgeschrieben. Er enthaelt bereits Zwischenstaende,
    Tests, Migrationen, offene Punkte und Anpassungen der Uebernahmeliste.
13. Der offene Abschnitt verwendet `In Arbeit`, `Zielcommit` und
    `Status: uncommitted`. Bei der Finalisierung werden Ueberschrift und
    Zielcommit auf die Commit-ID ohne `-x` vereinheitlicht, das Abschlussdatum und
    der konkrete Commit-Betreff gesetzt und der Statusvermerk entfernt.

Format eines Branch-Abschnitts:

  [3.1.12-paj-1-3-x] - In Arbeit seit <YYYY-MM-DD>
  Zielcommit: 3.1.12-paj-1-3
  Status: uncommitted

    Changed
    - [Software] ...

Finalisiert:

  [3.1.12-paj-1-2] - <YYYY-MM-DD>
  Commit: 3.1.12-paj-1-2 | Kommunikationsarchitektur abgesichert

    Added
    - [Software] ...

    Changed
    - [Infrastruktur] ...

  Dokumentationsuebernahme beim Main-Versionssprung

    Project/software/ARCHITEKTUR.txt
    - Authentifizierung, Rollen und persistente Sitzungen dokumentieren.

Format eines Main-Eintrags:

  [3.1.11] - <YYYY-MM-DD>
  Commit: 3.1.11 | Dokumentations- und Versionsworkflow vereinheitlicht
  Quelle: 3.1.10-paj-1

    Added
    - [Dokumentation] ...

    Changed
    - [Software] ...
    - [Infrastruktur] ...

    Fixed
    - [Software] ...

    Removed
    - [Repository] ...

Zulaessige Scopes:

  [Software] [Infrastruktur] [Dokumentation] [Repository] [Datenbank]

Das Changelog beschreibt die tatsaechlichen Aenderungen zwischen zwei
Main-Versionen. Der Commit-Betreff liefert nur die Zusammenfassung und ersetzt
nicht die einzelnen Changelog-Punkte.


================================================================================
7. ABLAUF BEI "DOKUMENTIEREN"
================================================================================

1. Aktuellen Branch, Arbeitsbaum, Diff und vorhandene Testergebnisse pruefen.
2. Tatsaechliche Aenderungen nach Added, Changed, Fixed und Removed einordnen.
3. Die fachliche Umsetzung und ihre Pruefungen erfolgen im `-x`-Stand.
4. Dokumentation und Finalisierung erfolgen erst auf ausdrueckliche Aufforderung
   des Users.
5. In einem Seitenbranch werden bei dieser Aufforderung der offene
   Branch-Changelogabschnitt und alle
   fuer diesen Branch ausdruecklich erlaubten Dokumentationsdateien vollstaendig
   aktualisiert.
6. Im Seitenbranch wird gleichzeitig die vorbereitete Version in
   Backend/package.json, Backend/package-lock.json und Branch-Changelog von
   `<naechste-Branch-Commit-ID>-x` auf `<naechste-Branch-Commit-ID>` finalisiert.
7. Diese Finalisierung erstellt noch keinen Commit und autorisiert weder Commit
   noch Push.
8. Nach der Finalisierung duerfen nur noch Abschlusspruefung, Vollstaendigkeits-
   kontrolle, ein ausdruecklich beauftragter Commit und ein gesondert
   beauftragter Push folgen.
9. Werden vor dem Commit weitere inhaltliche Aenderungen notwendig, muss zuerst
   wieder der zugehoerige `-x`-Stand in Paketdateien und Branch-Changelog
   hergestellt werden. Danach ist eine erneute Dokumentations- und
   Finalisierungsaufforderung erforderlich.
10. Kein Commit darf eine `-x`-Version enthalten.
11. Noch keine permanente Fachdokumentation und keine Main-Version anlegen.
12. Ein Seitenbranch muss vor einer verbindlichen Versionsfrage vollstaendig,
   getestet, committed, sauber und gepusht sein. Fehlen Commit oder Push, werden
   zuerst die jeweils gesonderten Freigaben eingeholt und ausgefuehrt.
13. Erst danach aktuelle Main-Version und Main-SHA neu pruefen und ausgeben:

     Aktuelle Main-Version: <version>
     Erkannte Aenderungsart: <MAJOR|MINOR|PATCH>
     Vorgeschlagene Version: <neue-version>
     Begruendung: <kurze Begruendung>

     Soll Version <neue-version> erstellt werden?

14. Antwortet der User mit Nein, bleiben nur die temporaeren Eintraege bestehen.
15. Bestaetigt oder aendert der User die konkrete Version, beginnt die
   Release-/Merge-Vorbereitung.
16. Bei einer direkten Main-Ausnahme ersetzt der Ablauf aus Abschnitt 10 die
   Punkte 5 bis 15 dieses Abschnitts.


================================================================================
8. DOKUMENTATION BEIM MAIN-VERSIONSSPRUNG
================================================================================

Die temporaeren Changelog-Dateien sind waehrend der Arbeit die einzige
fortlaufende Dokumentation. Fachliche Dokumentationsdateien werden erst bei der
bestaetigten Main-Version aktualisiert und beschreiben danach den neuen
aktuellen Main-Stand.

Organisatorische Ausnahmen sind Project/DokuVersGit.txt und AGENTS.md. Sie
duerfen nach ausdruecklicher Userfreigabe bereits im Seitenbranch aktualisiert
werden, damit Workflow- und Arbeitsregeln sofort fuer alle weiteren Commits
gelten. Diese Ausnahme erstreckt sich nicht auf Software-, Seiten-, Datenbank-
oder Server-Fachdokumentation.

Funktionale Code- und Konfigurationsdateien werden waehrend der Umsetzung normal
geaendert. Die Aufschubregel betrifft beschreibende Dokumentation, nicht
ausfuehrbare Konfigurationen oder Vorlagen, die fuer die Funktion erforderlich
sind.

  Aenderung                         | Beim Versionssprung pruefen/aktualisieren
  ----------------------------------|-------------------------------------------
  Jede Aenderung                    | Project/ChangeLogs/ChangeLog-main.txt
  Produktzweck, Zielgruppen,        | Project/FACHKONZEPT.txt
  Nutzungsszenarien, fachliche      |
  Anforderungen oder Abgrenzungen  |
  Datenbankschema                   | Project/software/DATENBANK.txt
  Endpoint oder Protokoll           | Project/software/ENDPOINTS.txt
  Architektur, Cache, State, Auth   | Project/software/ARCHITEKTUR.txt
  Bestehende HTML-Seite             | Exakte Zuordnung aus SOFTWARE-DOKU.txt;
                                    | Project/software/seiten/<dokumentname>.txt
  Neue/entfernte Seite              | Seitendoku, SOFTWARE-DOKU.txt, AGENTS.md
  Software-Dokumentstruktur         | Project/software/SOFTWARE-DOKU.txt
  Serverbetrieb                     | Project/server-configs/SERVER-SETUP.txt,
                                    | ggf. Project/server-configs/SERVER-DOKU.txt
  Caddy/systemd-Vorlagen            | Betroffene Vorlage und Serverdokumentation
  Projektueberblick oder Routing    | AGENTS.md
  Workflowregel                     | Project/DokuVersGit.txt

Project/2do/ wird nur bei direktem fachlichem Bezug aktualisiert.
Project/archive/ wird nie aktualisiert.
HTML-Dateinamen werden nicht automatisch in Dokumentnamen umgewandelt, da die
Gross-/Kleinschreibung einzelner Dateien abweicht.


================================================================================
9. SEITENBRANCH-WORKFLOW
================================================================================

Start:

1. Branch, Status, Diff und aktuellen Main-Stand pruefen.
2. main muss vor einer neuen Arbeit sauber und auf dem freigegebenen Stand sein.
   Ein Update erfolgt nur im autorisierten Git-Workflow mit Fast-Forward:

     git pull --ff-only origin main

3. Nach einem autorisierten git fetch den freien Branchnamen auch gegen Remote
   und Historie pruefen und Branch anlegen:

     git switch -c <Basisversion>-<Kuerzel>-<BranchId>

Arbeit im Seitenbranch:

1. Den verpflichtenden Beginn jeder neuen Arbeit aus Abschnitt 1 vollstaendig
   ausfuehren.
2. Implementieren, online beziehungsweise lokal testen und den offenen
   Changelogabschnitt samt dateigenauer Uebernahmeliste laufend fortschreiben.
3. Permanente Fachdokumentation unveraendert lassen; nur ausdruecklich
   freigegebene organisatorische Ausnahmen duerfen angepasst werden.
4. Nach fachlichem Abschluss alle vorgesehenen Pruefungen im `-x`-Stand
   ausfuehren.
5. Dokumentation und Finalisierung ausschliesslich nach Abschnitt 7 ausfuehren.
6. Git-Status, Gesamtdiff, Changelog, Uebernahmeliste und letzte Commits pruefen.
7. Die vollstaendige Menge aller auftragsbezogenen Dateien bestimmen und nach
   den Vollstaendigkeitsregeln aus Abschnitt 11 pruefen.
8. Alle und nur die auftragsbezogenen Dateien stagen; Secrets ausschliessen.
9. Staged Diff, unstaged Diff und untracked Dateien gemeinsam gegen den
   vollstaendigen Auftragsumfang pruefen.
10. Commit nur nach ausdruecklicher Freigabe im Branch-Format erstellen.
11. Nach dem Commit pruefen, dass Commit-ID, package.json, package-lock.json und
    GET-/version-Wert logisch uebereinstimmen und keine auftragsbezogene Datei
    im Arbeitsbaum zurueckblieb.
12. Push nur nach eigener ausdruecklicher Freigabe ausfuehren. Eine
    Commit-Freigabe beinhaltet keine Push-Freigabe.
13. Kein Rebase oder Force-Push eines veroeffentlichten Branches.

Vor der Integration:

1. Seitenbranch muss vollstaendig, getestet, sauber und gepusht sein.
2. main aktualisieren, dabei nur Fast-Forward zulassen.
3. Aktuelle Main-Version und Main-SHA erfassen.
4. Gesamten Integrationsumfang nach SemVer bewerten.
5. Zielversion vorschlagen und konkret bestaetigen lassen.
6. Vor dem Merge erneut pruefen, dass main und Remote-Branch unveraendert sind.

Integration:

1. Immer einen echten Merge-Commit erzwingen und die Finalisierung offenlassen:

     git merge --no-ff --no-commit <Branchname>

2. Bei unerwarteten Konflikten oder unklarem Umfang abbrechen und nachfragen.
3. Branch-Changelog in ChangeLog-main.txt uebertragen und zusammenfassen.
4. Jeden Punkt der Dokumentationsuebernahmeliste in die genannten permanenten
   Fach-, Seiten-, Server- und Uebersichtsdokumente einarbeiten und gegen den
   gesamten Branch-Diff pruefen.
5. Erst nach vollstaendiger Uebernahme das temporaere Branch-Log entfernen.
6. Backend/package.json und Backend/package-lock.json auf die bestaetigte
   Main-Version setzen; die Branch-Praerelease-Version darf nicht verbleiben.
7. Status, Diff, Changelog, permanente Dokumentation, Paketversion und Tests
   pruefen.
8. Sicherstellen, dass MERGE_HEAD existiert.
9. Merge-Commit mit exakt folgendem Format erstellen:

     <NeueVersion> | <Kurzkommentar>

Nach der Integration:

1. Pruefen, dass der Merge-Commit zwei Eltern besitzt.
2. Pruefen, dass Paketversion, Changelog und Commit-Version identisch sind.
3. Push von main separat freigeben lassen.
4. Nach erfolgreichem und verifiziertem Main-Push bleiben Remote- und lokaler
   Seitenbranch dauerhaft erhalten. Der integrierte Branch wird nicht fuer neue
   Arbeiten wiederverwendet.
5. Wird ein Push abgelehnt, im sicheren Stop-Zustand bleiben: den lokalen
   Merge-Commit nicht veraendern, nicht pullen, nicht resetten, nicht rebasen,
   nicht force-pushen und keine Branches oder Referenzen veraendern. Status und
   Abweichung melden.
6. Eine Wiederherstellung, zum Beispiel Sicherungsbranch plus erneute Integration
   auf aktuellem origin/main, wird als separater Plan vorgelegt und benoetigt
   eine neue ausdrueckliche Freigabe sowie eine neue Versionsbestaetigung.


================================================================================
10. DIREKTE MAIN-AUSNAHME
================================================================================

Direkte Main-Arbeit ist nur erlaubt, wenn der User sie vor der Aenderung
ausdruecklich als Ausnahme freigibt.

1. Bei sauberem main muss zuerst gefragt werden, ob ein Seitenbranch angelegt
   werden soll. Erst wenn der User dies ablehnt und die direkte Main-Ausnahme
   ausdruecklich freigibt, darf deren Vorbereitung beginnen.
2. Vor jeder inhaltlichen Aenderung wird die aktuelle Main-Patchversion um eins
   erhoeht und mit `-x` versehen. Dieser vorlaeufige Wert wird gemeinsam in
   Backend/package.json, Backend/package-lock.json und ChangeLog-main.tmp gesetzt.
3. ChangeLog-main.tmp erhaelt fuer diesen Stand einen offenen Abschnitt mit
   `In Arbeit`, der vorlaeufigen `Arbeitsversion`, `Zielversion: offen` und
   `Status: uncommitted`.
4. Der Patch-`-x`-Stand ist nur eine Arbeitsversion und keine vorweggenommene
   SemVer-Entscheidung. Alle Aenderungen werden darin bis zur ausdruecklichen
   Aufforderung `dokumentieren` erfasst und getestet.
5. Bei `dokumentieren` werden tatsaechlicher Umfang und SemVer-Stufe bewertet und
   die konkrete PATCH-, MINOR- oder MAJOR-Zielversion vorgeschlagen.
6. Erst nach Bestaetigung dieser Zielversion werden ChangeLog-main.txt,
   Fachdokumentation, package.json und package-lock.json finalisiert sowie
   ChangeLog-main.tmp uebertragen und entfernt.
7. Die Versionsbestaetigung autorisiert keinen Commit. Der normale Main-Commit
   lautet nach gesonderter Commit-Aufforderung
   `<NeueVersion> | <Kurzkommentar>`.
8. Es gibt keinen Zwischencommit und kein Commit mit `-x`.
9. Im zentralen Changelog lautet das Quellenfeld "direkte Main-Ausnahme".
10. Push bleibt ein eigener, ausdruecklich freizugebender Schritt.
11. Ist die Arbeit zu gross fuer einen einzelnen Versionscommit, muss stattdessen
    ein Seitenbranch verwendet werden.
12. Ein phasenabhaengiger Modell- oder Reasoning-Wechsel innerhalb derselben
    OpenCode-Session ist zulaessig, solange dadurch keine verpflichtende Doku-,
    Test-, Versions- oder Git-Pruefung aus dem verfuegbaren Kontext faellt.


================================================================================
11. SICHERHEIT UND GIT-HYGIENE
================================================================================

Vollstaendigkeit eines Commits:

1. Vor dem Staging werden Git-Status, Gesamtdiff, untracked Dateien, Loeschungen
   und Umbenennungen geprueft.
2. Aus dem freigegebenen Auftrag und dem Gesamtdiff wird die vollstaendige Menge
   aller auftragsbezogenen Dateien bestimmt.
3. Zu dieser Menge gehoeren auch neue, geloeschte, umbenannte und automatisch
   angepasste Dateien sowie verpflichtende Paket-, Lockfile-, Versions-,
   Dokumentations- und Changelog-Dateien.
4. Alle auftragsbezogenen Dateien muessen in denselben dafuer vorgesehenen Commit
   aufgenommen werden. Keine davon darf versehentlich unstaged oder untracked
   zurueckbleiben.
5. Unabhaengige Aenderungen des Users oder anderer Bearbeiter duerfen nicht
   aufgenommen, veraendert oder zurueckgesetzt werden.
6. Vor dem Commit werden staged Diff, unstaged Diff und untracked Dateien
   gemeinsam gegen den vollstaendigen Auftragsumfang geprueft. Secrets bleiben
   ausgeschlossen.
7. Der Commit wird ausschliesslich auf ausdrueckliche Aufforderung erstellt.
8. Nach dem Commit werden Git-Status und Commit-Diff erneut geprueft. Bleibt eine
   auftragsbezogene Datei zurueck oder fehlt sie im Commit, gilt der Commit als
   unvollstaendig.
9. Ein unvollstaendiger Commit darf nicht als abgeschlossen gemeldet und nicht
   gepusht werden. Der sichere Zustand und die fehlenden Dateien werden dem User
   gemeldet; eine Korrektur erfolgt nur im freigegebenen Workflow und niemals
   eigenmaechtig per Amend.
10. Ein Push wird ausschliesslich nach einer eigenen ausdruecklichen Aufforderung
    ausgefuehrt. Eine Commit-Freigabe beinhaltet keine Push-Freigabe.

Sicherheit:

Folgende Dateien und Inhalte duerfen niemals committed oder vollstaendig
ausgegeben werden:

  Backend/.env
  Backend/service-account.json
  sonstige Service-Account-JSON-Dateien
  private Schluessel, Tokens, Passwoerter und Session-Geheimnisse

Vor jedem Commit werden Dateinamen, staged Diff und .gitignore geprueft. Wird ein
Geheimnis committed, ist die Arbeit zu stoppen. Der Schluessel muss zuerst beim
Anbieter gesperrt oder rotiert werden. Eine Bereinigung der Git-Historie ist ein
separater destruktiver Vorgang und benoetigt eine ausdrueckliche Freigabe.

Bekannter historischer Befund:

  Die Commits 48c20e9, 4e2826a und 685d3b4 fuegten jeweils eine spaeter
  geloeschte Service-Account-JSON-Datei zur Git-Historie hinzu. Die operative
  Sperrung/Rotation und eine moegliche History-Bereinigung wurden vom User
  vorerst zurueckgestellt. Die Dateiinhalte duerfen nicht ausgelesen oder
  wiedergegeben werden.


================================================================================
12. ABSCHLUSSBERICHT
================================================================================

Nach Dokumentations-, Versions- oder Git-Arbeiten wird knapp berichtet:

1. Welche Dateien und Regeln geaendert wurden.
2. Welche Pruefungen erfolgreich waren oder nicht ausgefuehrt werden konnten.
3. Aktueller Branch und Arbeitsbaumstatus.
4. Erstellte Commit- und Versionsnummer, falls vorhanden.
5. Ob Push oder Merge noch ausstehen.
6. Ob alle auftragsbezogenen Dateien im Commit enthalten sind und ob noch
   auftragsbezogene Aenderungen im Arbeitsbaum verbleiben. Unabhaengige vorhandene
   Aenderungen werden getrennt benannt.
